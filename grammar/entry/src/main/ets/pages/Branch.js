"use strict";
/*
 * 分支渲染
 * */
struct Branch extends   {
    constructor(/*
     * 分支渲染
     * */ /*
 * 分支渲染
 * */
    ) {
    }
    build() {
            .height('100%');
    }
}
struct Branch_Comp extends   {
    constructor(/*
     * 分支渲染
     * */ /*
 * 分支渲染
 * */
    /*
     * 分支渲染
     * */
    ) {
        /*
         * 分支渲染
         * */
    }
    build() {
        ;
    }
}
//# sourceMappingURL=Branch.js.map